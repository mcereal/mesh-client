# Written by scripts/gen-{emoji,icons,font}.py - keep them out of diffs and language stats. The
# whole directory rather than a file per line, so a fourth generated table is covered the day it
# lands; without this the emoji table alone is 37k lines on top of every pull request.
src/ui/generated/** linguist-generated=true -diff
